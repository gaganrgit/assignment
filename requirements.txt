# Part A dependencies (standard library only — no extra install needed for Python 3.10+)
# unicodedata, json, re, pathlib, collections are all built-in

# Part B dependencies (for Colab)
transformers==4.51.0
peft==0.10.0
datasets==2.19.0
trl==0.8.6
bitsandbytes==0.43.0
accelerate==0.29.3
torch>=2.0.0
sentencepiece
